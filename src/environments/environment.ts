// src/environments/environment.ts

export const environment = {
  production: false,
  // URL de base de votre API Spring Boot (ajustez le port si nécessaire)
  apiUrl: 'http://localhost:8081/api',
};